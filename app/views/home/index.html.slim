.container.mx-auto.mt-28.px-5.py-5.flex.bg-gradient-to-r.from-sky-200.via-sky-100.to-white.rounded-lg
  .flex-col
    h2.text-xl.font-bold.mb-5 Posts

    - @posts.each do |post|
      .flex-col.mb-10.rounded-md.shadow-lg.bg-white.divide-y
        .flex.justify-between.p-1.text-sm.px-5.py-3.items-center
          div = post.user.email
          .text-gray-400.text-xs = time_ago_in_words(post.created_at)
        .p-10.font-bold.text-center
          = post.content
        .p-4.text-xs
          | 💗 2 Likes     ☁️ 10 Comments
